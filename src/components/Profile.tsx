import React from 'react'

function Profile() {
    return (
        <div>
            {/* TODO If user is not the owner then:
                show username, avatar, bio and list the links */}
                {/* TODO If user is the owner then:
                show all above fields along with edit options, add options to add links */}
                {/* TODO provide option to copy the links */}
                {/* TODO clikcing on the link should open the social media page of the user */}
                {/* TODO while adding links show a dropdown of popular social media websites and show a field to enter username */}
        </div>
    )
}

export default Profile
